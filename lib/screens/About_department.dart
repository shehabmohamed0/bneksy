import 'package:flutter/material.dart';
class About_department extends StatelessWidget {
  const About_department({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
